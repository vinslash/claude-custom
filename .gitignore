# macOS
.DS_Store
._*

# Secrets et config machine — ce repo est sur GitHub
.credentials.json
.env
.env.*
*.local.json

# État runtime de Claude Code, si un jour un dossier de ~/.claude atterrit ici
*.log
*.jsonl
cache/
file-history/
paste-cache/
session-env/
sessions/
shell-snapshots/
statsig/
todos/

# Montage local : CLAUDE.md à la racine est un symlink *sortant* vers
# ~/.claude/CLAUDE.md, qui ne porterait qu'un chemin absolu inutile ailleurs
/CLAUDE.md

# Brouillons et handoffs de session, propres à cette machine
drafts/

# Éditeurs
# (claude.code-workspace est versionné volontairement — ne pas ignorer *.code-workspace)
.vscode/
.idea/
